---
title: '2020-09-30'
author: "Yvonne"
date: "2020/9/30"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

## R Markdown

This is an R Markdown document. Markdown is a simple formatting syntax for authoring HTML, PDF, and MS Word documents. For more details on using R Markdown see <http://rmarkdown.rstudio.com>.

When you click the **Knit** button a document will be generated that includes both content as well as the output of any embedded R code chunks within the document. You can embed an R code chunk like this:

```{r cars}
summary(cars)
```

## Including Plots

You can also embed plots, for example:

```{r pressure, echo=FALSE}
plot(pressure)
```

Note that the `echo = FALSE` parameter was added to the code chunk to prevent printing of the R code that generated the plot.

```{r}
install.packages("devtools")
remotes::install_github("tpemartin/econDS", force=T)
```



```{r}
library(econDS); setup()
```

```{r}
install.packages(c("googlesheets4"))
```


```{r}
install.packages(c("googlesheets4","tidyverse"))
```
```{r}
Yvonne<-100
Yvonne=100
object_value=333
A<-object_value
```


```{r}
my_109_total_credits<-15
total_credits<-15
my.109.total_credits<-15
my.109.total.credits<-15
.my.108.total_credits <- 15


```

```{r}
Yvonne<-100
Grace<-Yvonne
```

```{r}
moon<-1001
.day.1001<-moon #記得開頭不能放數字
moon=1001
.day.1001=moon  #開頭不能放底線
.day_1001=moon
```

```{r}

```

